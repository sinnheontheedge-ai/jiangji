-- 此文件仅作为记录，实际的ORM模型将在database.py中添加
-- 需要为以下表创建ORM模型：
-- 1. state_transitions
-- 2. idempotency_keys  
-- 3. saga_logs
-- 4. notification_configs (已存在但名称不一致)

-- 这些表已经在001_init_database.sql中创建，只需要在database.py中添加对应的ORM类
